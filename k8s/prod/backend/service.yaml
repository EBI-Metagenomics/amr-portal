apiVersion: v1
kind: Service
metadata:
  name: amr-api-svc-prod
  namespace: amr-portal-ns-prod
  labels:
    ensembl.platform.app.name: amr-api-prod
spec:
  selector:
    ensembl.platform.app.name: amr-api-prod
  type: NodePort
  ports:
    - port: 8000
      protocol: TCP
      targetPort: 8000
      nodePort: 32025
