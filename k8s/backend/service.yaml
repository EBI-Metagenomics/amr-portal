apiVersion: v1
kind: Service
metadata:
  name: amr-api-svc-dev
  namespace: amr-portal-dev
  labels:
    ensembl.platform.app.name: amr
spec:
  selector:
    ensembl.platform.app.name: amr
  type: NodePort
  ports:
    - port: 8000
      protocol: TCP
      targetPort: 8000
      nodePort: 32027
