/**
 * ESM default export for CJS `prop-types` (used by MUI StyledEngineProvider).
 */
import * as propTypesModule from 'prop-types/index.js';
import { cjsDefaultExport } from './cjsDefault';

export default cjsDefaultExport(propTypesModule);
